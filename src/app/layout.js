import { Poppins } from "next/font/google";
import "./globals.css";

const poppins = Poppins({
  subsets: ["latin"],
  weight: ["100", "200", "300", "400", "500", "600", "700", "800", "900"],
});

export const metadata = {
  title: "Panto | Furniture store",
  description:
    "Explore Panto, your destination for stylish and durable furniture. Discover our curated selection of sofas, dining sets, and storage solutions that bring comfort and elegance to every space.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={poppins.className}>{children}</body>
    </html>
  );
}
